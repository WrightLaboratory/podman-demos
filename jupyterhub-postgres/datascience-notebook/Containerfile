ARG DOCKER_DATASCIENCE_NOTEBOOK_IMAGE_VERSION
FROM quay.io/jupyter/datascience-notebook:$DOCKER_DATASCIENCE_NOTEBOOK_IMAGE_VERSION

ARG JUPYTERHUB_VERSION
RUN python3 -m pip install --no-cache jupyterhub==$JUPYTERHUB_VERSION
